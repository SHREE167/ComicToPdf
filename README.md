# ComicScraper

📖 A simple desktop app to scrape comics and view them offline.

## 💻 How to Run

1. Clone this repo
2. Run `npm install`
3. Start with `npm start` or `run.bat`

## 📦 Build for Windows

```bash
npm run dist
